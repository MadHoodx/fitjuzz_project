import { StyleSheet } from "react-native"

const style = StyleSheet.create({
  container: {
    fontSize: '2rem'
  },
  button: {
    alignItems: 'center',
    backgroundColor: '#DDDDDD',
    padding: 10,
  },
})
